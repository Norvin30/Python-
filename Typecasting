# type casting = convert the data type of a value to another data type

x =12
y = 2.3
z = "12"

print(float(x))
print(int(y))
print(float(z))

'''we can concatenate a string with only a string not int or float. So in that condition we can use type casting to convert int,flaot to String.'''

print("x is : " + str(x))
print("y is: " + str(y))
