# Here is a program to identify whether the no. given by the user is even or odd.

a=float(input("Enter the value of variable a: "))

if a%2==0:
    print("a is a even no. ")
else:
    print("a is a odd no ")
